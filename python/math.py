import math

pi=3.14
x=1
y=2
z=3

#print(round(pi)) used for rounding off the give data
#print(math.ceil(pi)) its requiers math import and used to give the ceil 
#print(math.floor(pi))  ....................... used to give the floor
#print(abs(pi)) gives the absolute value
#print(pow(pi,2)) gives the output as power to which we have to raise (variable.exponential)
#print(math.sqrt(pi)) gives the square root value
#print(max(x,y,z))  give the max value of the data
#print(min(x,y,z))      gives the mini value of the data